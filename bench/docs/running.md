```sh
npm i
npm run start:node # or `npm run start` to run using the `ts-node/esm` Node.js loader, which transpiles the typescript files on demand
npm run gen-table # Uses the results that were generated by previous step, and saved into the `results` folder (one file per data source)
```